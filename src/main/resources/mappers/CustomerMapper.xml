<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper 
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.theonefive.customer.model.mapper.CustomerMapper">

	<select id="countByLoginId" parameterType="string" resultType="int">
		SELECT COUNT(*)
		FROM CUSTOMER
		WHERE LOGIN_ID = #{loginId}
	</select>
	
	<!-- 아이디 중복 확인 / 회원 조회 -->
	<select id="selectCustomerByLoginId" parameterType="string" resultType="CustomerDTO">
		SELECT ID, LOGIN_ID, PASSWORD, NAME, PHONE, EMAIL, 
			   MEMBERSHIP_GRADE, TOTAL_VISIT_COUNT, MEMO
		FROM CUSTOMER
		WHERE LOGIN_ID = #{loginId}
	</select>
	
	<insert id="insertCustomer" parameterType="CustomerDTO">
		INSERT INTO CUSTOMER (ID, LOGIN_ID, PASSWORD, NAME, PHONE, EMAIL)
		VALUES (CUSTOMER_SEQ.NEXTVAL, #{loginId}, #{password}, #{name}, #{phone},
				#{email})
	</insert>

<!-- 회원정보 수정 -->
	<update id="updateCustomer" parameterType="CustomerDTO">
		UPDATE CUSTOMER
		SET NAME = #{name},
			PHONE = #{phone}
		WHERE LOGIN_ID = #{loginId}
	</update>
	
<!-- 비밀번호 변경 -->
	<update id="updatePassword" parameterType="CustomerDTO">
		UPDATE CUSTOMER
		SET PASSWORD = #{newPassword}
		WHERE LOGIN_ID = #{loginId}
	</update>
</mapper>