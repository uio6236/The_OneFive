<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="package com.theonefive.room.model.mapper.RoomMapper">
	<select id="selectRoomList" parameterType="" resultType="RoomDTO">
	
	</select>
	<select id="selectRoomById" parameterType="int" resultType="RoomDTO">
	
	</select>
	<select id="selectRoomByFilter" parameterType="" resultType="RoomDTO">
	
	</select>
	<select id="selectAvailableRooms" parameterType="int" resultType="RoomDTO">
	
	</select>
	<update id="updateRoomStatus" parameterType="">
	
	</update>
</mapper>
